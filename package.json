{
  "name": "pr-workflow-manager",
  "version": "1.0.0",
  "description": "GitHub Action to manage PR workflows",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
} 